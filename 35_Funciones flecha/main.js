// Funciones flecha:

// BÁSICO:
// Si solo reciben un parámetro, no hace falta ponerlo entre (paréntesis)
// Si solo tienen una línea de código, no hace falta ponerla entre {llaves}
// Si solo tienen una línea de código, no se usan llaves, y se escribe en una sola línea, no se debe usar "return" ya que el return está por defecto

// AVANZADO:
// No se las recomienda para utilizar como métodos y no se pueden utilizar como constructores
// Avanzar con el siguiente capítulo: this.