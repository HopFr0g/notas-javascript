// Pestañas de google chrome que contienen herramientas para desarrolladores
// Todas las pestañas corresponden a la sección de "inspeccionar" o "inspeccionar elemento"

// Pestaña element:
// Muestra el contenido HTML y permite seleccionar los elementos, copiarlos, copiar los selectores para CSS, copiar los selectores para el método .querySelector(), copiar su estilo, editarlos, permite forzar estados como :hover, :focus, entre otros, eliminarlos, etc...

// Pestaña sources o resources:
// Nos muestra los archivos, permite modificarlos y guardarlos desde el navegador. Permite crear snippets (son fragmentos de código que se guardan en el navegador) para ejecutarlos todas las veces que necesitemos.

// Pestaña network:
// Permite visualizar cuánto tardan en cargar los distintos recursos de la página. Permite visualizar cuál es el origen de cada solicitud, y un resumen del tiempo total en que cargó la página, solicitudes totales, etc. Permite bloquear la carga de un archivo, y filtrar u ordenar la lista según el tipo de archivos u otros parámetros.

// Pestaña timeline o performance:
// Permite visualizar el porcentaje de los recursos utilizados (procesador, memoria, internet, etc) y grabar los mismos.

// Pestaña application:
// Da información sobre la aplicación.

// Pestaña console:
// Muestra errores, logs, etc. Permite filtrar qué información queremos ver en la consola. Podemos utilizarla para ejecutar código de javascript.